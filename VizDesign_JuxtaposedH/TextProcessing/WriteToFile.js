
/*
const fs = require('fs');
fs.writeFile('Output.txt', hdata, (err) => {

    // In case of a error throw err.
    if (err) throw err;
});
*/


/* 

// Import http and fs 
const fs = require('fs')
const http = require('http')

// Creating Server 
http.createServer((req, res) => {
    console.log(res);
    fs.writeFile('Output.txt', "hii", (err) => {

        // In case of a error throw err.
        if (err) throw err;
    });

}).listen(8080, 'localhost'); */